[project]
name = "her"
version = "0.1.0"
description = "A proactive, personified agent that explores information autonomously"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
    "openai>=1.50.0",
    "httpx>=0.27.0",
    "feedparser>=6.0.0",
    "rich>=13.0.0",
    "prompt-toolkit>=3.0.0",
]

[project.scripts]
her = "her.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
