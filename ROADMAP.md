# Her - 重构路线图

## 📊 当前进度

**总体进度**: 85%

- ✅ **Phase 1**: 基础重构 (100%)
- ✅ **Phase 2**: 核心层实现 (100%)
- ✅ **Phase 3**: 基础设施层 (100%)
- ✅ **Phase 4**: TUI 分栏界面 (100%)
- 🚧 **Phase 5**: 测试和优化 (0%)

**核心功能状态**:
- ✅ TUI 分栏界面（Textual）
- ✅ 记忆系统（SQLite 持久化）
- ✅ RSSHub 集成（多 route 支持）
- ✅ LLM 抽象（OpenAI/Anthropic）
- ✅ 意图识别系统（基于 LLM）
- ✅ 后台异步探索
- ✅ 主动分享机制
- ✅ 事件系统（EventBus）
- ❌ 工具调用框架（可选，MVP 不需要）

**可用状态**: ✅ 代码可运行

---

## 🎯 项目愿景

## 🎯 项目愿景

**Her** 是一个拟人化的自主探索 Agent。她不仅仅是一个被动的聊天机器人，而是一个有"主动性"的数字伙伴：
- **主动探索**: 通过 RSSHub 获取信息，基于自己的"意图"决定探索方向
- **自然交互**: 像朋友一样聊天，可以随时打断用户、追加话题
- **意图驱动**: 所有行为基于她自己的意图识别，她决定怎么做

---

## 📋 重构目标

1. **业务/底层解耦** - 核心逻辑与 UI 完全分离
2. **GUI 展示** - 分栏界面：左侧意图记录 + 右侧对话区
3. **RSSHub 集成** - 作为主要信息源
4. **拟人化交互** - 打断、追加话题等主动行为
5. **开源级代码质量** - 清晰的目录结构和文档

---

## 🏗️ 新架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                           GUI (TUI 分栏)                             │
│  ┌──────────────────────┐  ┌───────────────────────────────────┐   │
│  │    意图记录面板       │  │         对话区 + 输入框            │   │
│  │   (Left Panel)       │  │        (Right Panel)              │   │
│  └──────────────────────┘  └───────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                                    ▲
                                    │ Events
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         Core (核心层)                                │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌────────────┐ │
│  │   Agent     │  │   Intent    │  │   Memory    │  │   Tools    │ │
│  │ (对话核心)   │  │  (意图识别)  │  │  (记忆系统)  │  │ (工具调用)  │ │
│  └─────────────┘  └─────────────┘  └─────────────┘  └────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                                    ▲
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      Infrastructure (基础设施层)                      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                  │
│  │     LLM     │  │   RSSHub    │  │  EventBus   │                  │
│  │  (模型抽象)  │  │  (信息源)    │  │  (事件总线)  │                  │
│  └─────────────┘  └─────────────┘  └─────────────┘                  │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 📁 新目录结构

```
her/
├── ROADMAP.md              # 路线图（本文件）
├── CLAUDE.md               # 项目说明
├── pyproject.toml
│
├── her/                    # 主模块
│   ├── CLAUDE.md
│   ├── __init__.py
│   ├── main.py             # 入口 ✅
│   │
│   ├── core/               # 核心层（业务逻辑）
│   │   ├── CLAUDE.md
│   │   ├── __init__.py     # 模块导出 ✅
│   │   ├── agent.py        # Agent 主类 ✅
│   │   ├── intent.py       # 意图识别 ✅
│   │   ├── memory.py       # 记忆系统 ✅
│   │   └── tools.py        # 外部工具调用 ⚪️（可选）
│   │
│   ├── infra/              # 基础设施层
│   │   ├── CLAUDE.md
│   │   ├── __init__.py     # 模块导出 ✅
│   │   ├── llm.py          # LLM 抽象 ✅
│   │   ├── events.py       # 事件系统 ✅
│   │   └── rsshub.py       # RSSHub 客户端 ✅
│   │
│   └── ui/                 # UI 层
│       ├── CLAUDE.md
│       ├── __init__.py
│       ├── app.py          # TUI 应用主入口 ✅
│       └── panels/         # UI 面板
│           ├── intent.py   # 左侧意图面板 ✅
│           └── chat.py     # 右侧对话面板 ✅
│
└── tests/                  # 测试 ❌（待添加）
    ├── unit/
    └── integration/
```

---

## 🔑 核心特性

### 1. 意图系统 (Intent System)

Her 的核心是"意图识别"。她会：
- 分析对话上下文，理解用户真正想要什么
- 生成自己的"想法"和"意图"
- 基于意图决定下一步行动

```python
@dataclass
class Intent:
    type: IntentType      # 意图类型
    reason: str           # 为什么产生这个意图
    action: str           # 计划的行动
    confidence: float     # 置信度
```

### 2. 主动探索 (Proactive Exploration)

通过 RSSHub 获取信息，基于意图过滤和推荐：
- 后台周期性获取内容
- 根据用户兴趣过滤
- 发现有趣内容时主动分享

### 3. 拟人化交互

- **打断**: 发现重要信息时可以打断对话
- **追加话题**: 聊天中可以引入新话题
- **记忆延续**: 记住之前聊过的内容

---

## 📅 实施阶段

### Phase 1: 基础重构 ✅

1. [x] 分析现有代码
2. [x] 创建新目录结构
3. [x] 清理无用文件（`heart/`, `mind/`, `senses/`, `soul/`）
4. [x] 迁移核心代码到新结构

### Phase 2: 核心层实现 ✅

1. [x] 重构 `core/agent.py` - 简化代理逻辑
2. [x] 优化 `core/memory.py` - 增强记忆系统
3. [x] 实现 `core/intent.py` - 意图识别系统（支持用户意图和自主意图）
4. [ ] 实现 `core/tools.py` - 工具调用框架（可选）
5. [x] 添加 `core/__init__.py` - 模块导出

### Phase 3: 基础设施层 ✅

1. [x] 重构 `infra/llm.py` - LLM 抽象（支持 OpenAI/Anthropic）
2. [x] 优化 `infra/rsshub.py` - RSSHub 客户端（支持多个 route）
3. [x] 实现 `infra/events.py` - 事件系统（EventBus）
4. [x] 添加 `infra/__init__.py` - 模块导出

### Phase 4: TUI 分栏界面 ✅

1. [x] 实现 `ui/app.py` - 使用 Textual 实现分栏布局
2. [x] 实现 `ui/panels/intent.py` - 左侧意图面板
3. [x] 实现 `ui/panels/chat.py` - 右侧对话面板
4. [x] 集成异步输入输出
5. [x] 实现快捷键（Ctrl+C 退出，Ctrl+E 探索）

### Phase 5: 测试和优化 🚧

1. [ ] 单元测试
2. [ ] 集成测试
3. [ ] 性能优化
4. [ ] 文档完善

---

## 🛠️ 技术选型

| 组件 | 选择 | 原因 |
|------|------|------|
| TUI | **Rich + Textual** | 内置分栏布局，Python 原生 |
| 事件 | **AsyncIO Events** | 轻量级，无外部依赖 |
| LLM | **OpenAI/Claude API** | 保持现有支持 |
| 信息源 | **RSSHub** | 一站式内容聚合 |

---

## 📝 待决策

1. ~~**TUI vs GUI**: 是否使用 Textual（更强大）还是保持 Rich（更简单）？~~
   - ✅ **已决定**: 使用 Textual，对分栏支持很好

2. ~~**意图持久化**: 意图记录是否需要持久化？~~
   - ✅ **已决定**: 先在内存中，后续可加 SQLite

3. ~~**RSSHub 配置**: 用户如何配置 RSSHub 源？~~
   - ✅ **已决定**: 使用代码配置，预设 TECH_ROUTES、NEWS_ROUTES 等

## 🔜 下一步计划

### 功能增强（可选）

1. **实现 `core/tools.py`**
   - 工具调用框架
   - 支持扩展外部工具（搜索、计算等）
   - 当前使用 LLM 直接生成响应，工具调用是进阶功能

2. **增强意图系统**
   - 更精细的意图分类
   - 意图链式处理
   - 意图冲突解决

### 测试和优化

3. **添加基础测试**
   - `tests/` 目录结构
   - 核心功能单元测试
   - 集成测试

4. **性能优化**
   - RSSHub 请求并发优化
   - 记忆查询优化
   - LLM 调用缓存

5. **文档完善**
   - API 文档
   - 使用指南
   - 架构设计文档
