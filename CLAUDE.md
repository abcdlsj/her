# Her - 拟人化自主探索 Agent

## 项目定位

一个具有**主动性**的拟人化 Agent。她能够：
- 自主探索互联网上的信息
- 在后台异步获取内容，适时主动分享发现
- 与用户自然交互，像朋友一样聊天

## 核心架构

```
┌─────────────────────────────────────────────────────────────┐
│                         CLI / UI                            │
│                    (终端 / Web / IDE)                        │
└─────────────────────────────────────────────────────────────┘
                              ▲
                              │ Event Stream
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                        EventBus                             │
│              (事件总线 - 解耦 Agent 和 UI)                    │
└─────────────────────────────────────────────────────────────┘
                              ▲
          ┌───────────────────┼───────────────────┐
          │                   │                   │
          ▼                   ▼                   ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐
│      Her        │ │    Explorer     │ │     Memory      │
│   (对话核心)     │ │  (异步探索器)    │ │   (记忆系统)     │
└─────────────────┘ └─────────────────┘ └─────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │     Sources     │
                    │  (信息源插件)    │
                    │ HN / RSS / ...  │
                    └─────────────────┘
```

## 目录结构

```
her/
├── CLAUDE.md              # 项目规范（本文件）
├── her/
│   ├── CLAUDE.md          # 核心模块规范
│   ├── agent.py           # Her Agent 主类
│   ├── cli.py             # CLI 入口
│   ├── llm.py             # LLM 抽象层
│   ├── memory.py          # 记忆系统
│   ├── prompts.py         # Prompt 模板
│   ├── events/            # 事件系统
│   │   ├── CLAUDE.md
│   │   ├── bus.py         # 事件总线
│   │   └── types.py       # 事件类型定义
│   └── sources/           # 信息源
│       ├── CLAUDE.md
│       ├── base.py        # Source 接口
│       ├── rss.py
│       └── hackernews.py
└── pyproject.toml
```

## 核心原则

### 1. 主动性优先
Her 不是被动响应的工具，而是有主动性的存在：
- 后台持续探索信息
- 发现有趣内容时主动分享
- 记住用户兴趣，推送相关内容

### 2. 事件驱动
所有状态变化通过事件通知：
- Agent 和 UI 通过 EventBus 解耦
- 支持多种 UI 形态
- 事件可持久化用于回放

### 3. 接口抽象
- Source: 信息源统一接口
- LLM: 模型提供商统一接口
- Event: 事件类型统一定义

### 4. 简单可靠
- 优先选择简单方案
- 错误处理要完善
- 避免过度抽象

## 代码风格

- Python 3.11+
- 类型注解必须
- 每个目录有自己的 CLAUDE.md
- 可读性 > 简洁性 > 性能

## 运行

```bash
uv sync
uv run her
```
