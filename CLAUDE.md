# Her - 拟人化自主探索 Agent

## 项目定位

一个具有**主动性**的拟人化 Agent。她能够：
- 自主探索互联网上的信息（通过 RSSHub）
- 基于自己的意图识别决定行动
- 与用户自然交互，可以打断、追加话题
- 在后台异步获取内容，适时主动分享发现

## 架构概览

```
┌─────────────────────────────────────────────────────────────────────┐
│                           UI (TUI 分栏)                              │
│  ┌──────────────────────┐  ┌───────────────────────────────────┐   │
│  │    意图记录面板       │  │         对话区 + 输入框            │   │
│  │   (Left Panel)       │  │        (Right Panel)              │   │
│  └──────────────────────┘  └───────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         Core (核心层)                                │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌────────────┐ │
│  │   Agent     │  │   Intent    │  │   Memory    │  │   Tools    │ │
│  │ (对话核心)   │  │  (意图识别)  │  │  (记忆系统)  │  │ (工具调用)  │ │
│  └─────────────┘  └─────────────┘  └─────────────┘  └────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      Plugins (插件层)                                │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                  │
│  │   RSSHub   │  │  WebSearch  │  │   LocalDoc  │                  │
│  │ (RSS 信息源) │  │ (联网搜索)   │  │ (本地文档)   │                  │
│  └─────────────┘  └─────────────┘  └─────────────┘                  │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      Infra (基础设施层)                              │
│  ┌─────────────┐  ┌─────────────┐                                   │
│  │     LLM     │  │   Events    │                                   │
│  │  (模型抽象)  │  │  (事件总线)  │                                   │
│  └─────────────┘  └─────────────┘                                   │
└─────────────────────────────────────────────────────────────────────┘
```

## 目录结构

```
her/
├── CLAUDE.md              # 项目规范（本文件）
├── ROADMAP.md             # 路线图
├── pyproject.toml
│
├── her/
│   ├── __init__.py
│   ├── main.py            # 入口
│   │
│   ├── core/              # 核心层（业务逻辑）
│   │   ├── CLAUDE.md
│   │   ├── agent.py       # Her Agent 主类
│   │   ├── intent.py      # 意图识别系统
│   │   ├── memory.py      # 记忆系统
│   │   └── tools.py       # 工具调用框架
│   │
│   ├── plugins/           # 插件层（信息源扩展）
│   │   ├── __init__.py
│   │   ├── base.py        # 插件基类
│   │   └── rsshub/        # RSSHub 插件
│   │       ├── __init__.py
│   │       └── plugin.py
│   │
│   ├── infra/             # 基础设施层
│   │   ├── CLAUDE.md
│   │   ├── llm.py         # LLM 抽象层
│   │   ├── events.py      # 事件系统
│   │   ├── rsshub.py      # RSSHub 客户端（已废弃，保留向后兼容）
│   │   ├── rss_sources_manager.py
│   │   └── search_topics_manager.py
│   │
│   └── ui/                # UI 层
│       ├── CLAUDE.md
│       ├── app.py         # Textual TUI 应用
│       └── panels/
│           ├── intent.py  # 左侧意图面板
│           └── chat.py    # 右侧对话面板
│
└── tests/
```

## 核心原则

### 1. 意图驱动
Her 的核心是意图识别。她会：
- 分析对话上下文，理解用户真正想要什么
- 生成自己的"想法"和"意图"
- 基于意图决定下一步行动

### 2. 四层分离
- **Core**: 业务逻辑，不依赖具体 UI 或基础设施
- **Plugins**: 信息源扩展（RSSHub、WebSearch、LocalDoc 等）
- **Infra**: 基础能力（LLM、事件）
- **UI**: 用户界面（Textual TUI）

### 3. 拟人化交互
- 可以打断对话分享新发现
- 可以追加话题
- 记住之前聊过的内容

## 运行

```bash
# 安装依赖
uv sync

# 运行
uv run her
```

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl+C | 退出 |
| Ctrl+E | 探索 |

## 代码风格

- Python 3.11+
- 类型注解必须
- 每个目录有自己的 CLAUDE.md
- 可读性 > 简洁性 > 性能
